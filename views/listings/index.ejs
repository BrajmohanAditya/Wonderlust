
<% layout("/layouts/boilerplate") %>  <!--  linked common boiler plate-->
<body>
    <h3>All listings</h3>
    <!--we are demanding something so get request of action it will give us a page, -->
    <!--Route No: 2 per request send karaga, or wo ak form render karaga new.ejs, we commented out bcg navbar k ander jo addlisting hai woha seh ya kaam ho jaa raha-->

    <!-- <form method="Get" action="/listings/new"> 
        <button>Create New Listing</button>   
    </form> -->

<!-- <%for(let listing of allListings){%>    
<div class="card" style="width: 18rem;">
  <img src="<%=listing.image%>" class="card-img-top" alt="listing_image">
  <div class="card-body">
    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card’s content.</p>
  </div>
</div>
<%}%>  -->

    <!-- <%for(let listing of allListings){%>
        <li><a href="/listings/<%= listing._id %>"> <%= listing.title %> </a></li>
    <%}%>  -->

     <!--Route No : 1 seh local host ka through  jb ya page run hoga , toh aha allListing meh sara data aa jayaga
           or uska ander her data set ko listing consider kiya gaya hai. or ush seh uska title and id extract kiya gaya hai 
           you can see title but not id. 

           # now when you click here it will triger Route No: 3, 
    
    -->
<!-- <% for(let listing of allListings) { %>
  <div class="card" style="width: 18rem; margin: 10px;">
    <a href="/listings/<%= listing._id %>">
      <img src="<%= listing.image %>" class="card-img-top" alt="listing_image">
    <div class="card-body">
      <p class="card-text">
        <%= listing.title %> <br>
        <%= listing.price %>
      </p>
    </div>
    </a>
  </div>
<% } %> -->
<% for(let listing of allListings) { %>
  <div class="card" style="width: 18rem; margin: 10px;">
    <a href="/listings/<%= listing._id %>">
      <img src="<%= listing.image %>" class="card-img-top" alt="listing image" style="width: 100%; height: auto;">
    </a>
    <div class="card-body">
      <p class="card-text">
        <%= listing.title %> <br>
        ₹<%= listing.price %>
      </p>
    </div>
  </div>
<% } %>
</body> 
<!-- 
* sara list ko print karaya  
* Post request if we submit something
-->